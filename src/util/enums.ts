export enum Operations {
  DELETE = 'delete',
  GET = 'get',
  HEAD = 'head',
  OPTIONS = 'options',
  PATCH = 'patch',
  POST = 'post',
  PUT = 'put'
}

export enum MockParam {
  STATUS = 'x-mock-status',
  MEDIATYPE = 'x-mock-mediatype',
  DISCRIMINATOR = 'x-mock-discriminator',
  RESPONSETYPE = 'x-mock-responsetype'
}
